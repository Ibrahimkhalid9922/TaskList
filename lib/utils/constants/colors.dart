import 'package:flutter/material.dart';

class AppColor {
  static const themeColor = Color.fromARGB(255, 1, 26, 26);
  static const textColor = Colors.white;
  static const buttonColor = Colors.red;
}
